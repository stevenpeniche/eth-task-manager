{
  "name": "eth-task-manager",
  "workspaces": [
    "web",
    "core"
  ],
  "scripts": {
    "dev": "concurrently --names \"WEB,CORE\" -c \"blue,yellow\" \"npm run dev -w web\" \"npm run dev -w core\""
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
